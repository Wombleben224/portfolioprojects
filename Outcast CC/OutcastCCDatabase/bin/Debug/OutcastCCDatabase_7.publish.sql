/*
Deployment script for OutcastCC

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "OutcastCC"
:setvar DefaultFilePrefix "OutcastCC"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL14.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL14.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Events].[Location] on table [dbo].[Events] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Events])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping unnamed constraint on [dbo].[Events]...';


GO
ALTER TABLE [dbo].[Events] DROP CONSTRAINT [DF__Events__EventDat__3C69FB99];


GO
PRINT N'Starting rebuilding table [dbo].[Events]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Events] (
    [BlogPostId]  INT             IDENTITY (1, 1) NOT NULL,
    [Title]       NVARCHAR (100)  NOT NULL,
    [Location]    NVARCHAR (100)  NOT NULL,
    [EventDate]   DATETIME        DEFAULT (GETDATE()) NOT NULL,
    [Text]        NVARCHAR (4000) NOT NULL,
    [Going]       INT             NULL,
    [PdfFileType] VARCHAR (100)   NULL,
    [PdfFileName] VARCHAR (100)   NULL,
    PRIMARY KEY CLUSTERED ([BlogPostId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Events])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Events] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Events] ([BlogPostId], [Title], [EventDate], [Text], [Going], [PdfFileType], [PdfFileName])
        SELECT   [BlogPostId],
                 [Title],
                 [EventDate],
                 [Text],
                 [Going],
                 [PdfFileType],
                 [PdfFileName]
        FROM     [dbo].[Events]
        ORDER BY [BlogPostId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Events] OFF;
    END

DROP TABLE [dbo].[Events];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Events]', N'Events';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Update complete.';


GO
