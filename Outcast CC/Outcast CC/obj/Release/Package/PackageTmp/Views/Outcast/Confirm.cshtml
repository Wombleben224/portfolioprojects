@model Outcast_CC.Models.AppUser
@{
  ViewBag.Title = "Confirm";
}

  <div class="p-5 m-3 bg-light min-vh-100 d-block">
    @if (Model != null && Model.IsConfirmed)
    {
      <h2>Subscribed!</h2>
      <p>Your email address is confirmed. Welcome to the group!</p>
    }
    else
    {
      <h2 class="text-danger">Failed!</h2>
      <p class="text-danger">Your email address has not been confirmed.</p>
    }

  @Html.ValidationSummary(false, null, new { @class = "text-danger" })
  </div>